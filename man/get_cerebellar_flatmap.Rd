% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cerebellar.R
\name{get_cerebellar_flatmap}
\alias{get_cerebellar_flatmap}
\title{Get SUIT cerebellar flatmap mesh}
\usage{
get_cerebellar_flatmap(surface = .cerebellar_surfaces)
}
\arguments{
\item{surface}{Surface type. Currently only \code{"suit_flat"}.}
}
\value{
A list with \code{vertices} (data.frame with x, y, z) and \code{faces}
(data.frame with i, j, k, 0-based indices matching \code{ggseg.formats}
convention for cerebellar meshes).
Has attribute \code{face_index_base = 0L}.
}
\description{
Retrieves the SUIT cerebellar flatmap surface mesh. This is a 2D flattened
representation of the cerebellar cortex, useful for visualising cerebellar
parcellations without 3D rendering.
}
\details{
The flatmap has z-coordinates near zero (flat projection). Vertex count
matches the SUIT 3D pial surface in \code{ggseg.formats}, so vertex indices
from cerebellar atlases map directly to this mesh.
}
\examples{
mesh <- get_cerebellar_flatmap()
nrow(mesh$vertices)
}
